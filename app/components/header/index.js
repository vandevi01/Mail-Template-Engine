import React from 'react';
import {
    Container,
    AppTitle
} from './styles';

export default () => {
    return (
        <Container>
            <AppTitle>Template Configure Engine</AppTitle>
            <div className="clear"></div>
        </Container>
    );
}
