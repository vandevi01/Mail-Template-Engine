import styled from 'styled-components'
import {AppButton} from '../../../generic.styles';

const ContainerForm = styled.form`
    margin-bottom: 10px;
    label {
        display: block;
        line-height: 30px;
    }
    input {
        margin-bottom: 10px;
    }
`;

export {ContainerForm, AppButton};
